function dydt = WGM(t,y)
g = 9.81; 
nu = 1e-3;
beta = 1e-3;
kappa = 1e-3;
k = 1;
a = 1
omega = sqrt(g * k);
etax = - a*k*sin(k * y(1)-omega * t);
z = 5; 
d = z;
hx = - a*k*exp(-k * z)*sin(k * y(3) - omega * t);
dydt(1) = y(2);
dydt(2) = -g * etax - kappa * (y(1)-y(3)-d)+nu * y(2);
dydt(3) = y(4); 
dydt(4) = -g * hx + kappa * (y(1)-y(3)-d) + beta * abs(y(3)); 
dydt=dydt';